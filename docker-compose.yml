services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - .env
    environment:
      - ALLOWED_ORIGINS=http://localhost:8501,http://127.0.0.1:8501
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
